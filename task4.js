var a = isNaN('11');
var b = isNaN(2-10);

console.log (a,b);

// Both of the result will given False because isNaN is checking whether '11' and -8 are number or not. Since both are numbers so it returns False as result.